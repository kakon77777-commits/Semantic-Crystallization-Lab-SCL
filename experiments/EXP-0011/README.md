# EXP-0011 — Closed-Loop Reflexive Strategy

This experiment extends EXP-0010 from static second-order triggers to a persistent opponent-response model. Reproduce with `python scripts/run_exp0011.py`; full formal results are in `results/report.md` and `VALIDATION_V11.json`.
